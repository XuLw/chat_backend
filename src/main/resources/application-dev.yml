server:
  port: 8080
  tomcat:
    uri-encoding: utf-8
    max-threads: 500

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/zjs_chat_test?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC


mybatis:
  mapper-locations: classpath:mapping/*Mapping.xml
  type-aliases-package: com.example.chat.mapper

#showSql
logging:
  level:
    root: debug
